name: Web security

on:
  pull_request:
    paths:
      - 'website/.htaccess'
      - 'website/play/**'
      - 'deploy/**'
      - '.github/workflows/deploy-metaserver-droplet.yml'
      - '.github/workflows/web-security.yml'
  push:
    branches: [main]
    paths:
      - 'website/.htaccess'
      - 'website/play/**'
      - 'deploy/**'
      - '.github/workflows/deploy-metaserver-droplet.yml'
      - '.github/workflows/web-security.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-browser-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Validate CSP and artifact integrity
        run: |
          python3 deploy/check-web-security.py
          python3 deploy/test-web-sync.py
          python3 deploy/test-play-transfer.py
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test compressed download progress
        run: node --test deploy/test-loading-progress.mjs
